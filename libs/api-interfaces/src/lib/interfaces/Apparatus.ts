// import { ExtendedData } from './ExtendedData';

export class Apparatus {
  car_id: string;
  distance: number;
  // extended_data: Partial<ExtendedData>;
  geohash: string;
  personnel: [];
  shift: string;
  station: string;
  unit_id: string;
  unit_type: string;
  unit_status: object;
}
